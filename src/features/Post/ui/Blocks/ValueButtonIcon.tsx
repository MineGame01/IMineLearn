import { styled, Typography } from '@mui/material'

export const ValueButtonIcon = styled(Typography)({
    fontSize: '1.1rem',
    fontWeight: '700',
})

ValueButtonIcon.defaultProps = {
    color: 'textSecondary',
    variant: 'button',
}
